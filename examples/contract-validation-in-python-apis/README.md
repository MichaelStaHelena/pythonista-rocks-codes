# Contract validation in Python APIs

Runnable examples for the article "Contract validation in Python APIs" (Contract validation in Python APIs).
Every snippet was executed and verified at generation time; the
`# =>` comments show the actual output.

## Run

```bash
pip install -r requirements.txt
python example_01.py
python example_02.py
```
