# Retries and timeouts with httpx in Python

Runnable examples for the article "Retries and timeouts with httpx in Python" (Retries and timeouts with httpx in Python).
Every snippet was executed and verified at generation time; the
`# =>` comments show the actual output.

## Run

```bash
pip install -r requirements.txt
python example_01.py
python example_02.py
```
